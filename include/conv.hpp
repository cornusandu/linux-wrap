#pragma once

#include "headers.hpp"
#include <vector>

template <typename T>
using DArray = std::vector<T>;

template <typename T>
using POINT = T*;

using DWORD = uint32_t;
using QWORD = uint64_t;
using SQWORD = int64_t;
